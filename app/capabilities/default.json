{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for the Hope desktop App Shell window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
