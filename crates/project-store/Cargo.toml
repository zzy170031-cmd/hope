[package]
name = "project-store"
version = "0.1.0"
edition = "2024"

[dependencies]
core-domain = { path = "../core-domain" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
path = "src/lib.rs"
