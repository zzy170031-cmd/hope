# v0.2 Scope Gate Readiness 2026-04-22

## Route

- repo: `E:\codex\hope`
- branch: `codex/contracts-freeze`
- anchor before this memo: `10796fe` (`docs: accept V3 golden sample overlay proposal`)
- route before this memo: `RC_READY` plus scope freeze
- memo type: main-control readiness / implementation-order planning

This memo does not open v0.2 implementation by itself. It records the current
readiness state and the exact thread order to use if the control thread opens a
future v0.2 scope gate.

## Named Thread Ledger

Use these names for tracking:

- `Hope总控-v0.2ScopeGateReadiness【主控规划中】`
- `Hope主线-RCPublicationConfirmation【RC_READY守护中】`
- `Hope-KB-黄金样本库Staging【知识库革命·已推送】`
- `Hope-V3字段线程-GoldenSampleOverlay【v0.2 Proposal已接收待命】`
- `Hope桌面端-WriterReadiness【InputBoundary待命】`
- `Hope接入线程-Qwen分镜生成Contract【Contract边界待命】`

## Current Anchors

Hope main:

- branch: `codex/contracts-freeze`
- anchor: `10796fe` (`docs: accept V3 golden sample overlay proposal`)
- state: clean and aligned with `origin/codex/contracts-freeze`

Hope KB:

- branch: `codex/contracts-freeze`
- anchor: `21d5641` (`Normalize golden sample intake staging`)
- state: clean and aligned with `origin/codex/contracts-freeze`

V3/V4 proposal:

- branch: `origin/codex/v3-field-overlay-proposal`
- anchor: `7a08cd6` (`docs: add golden sample v0.2 overlay proposal`)
- state: docs-only proposal accepted as planning input

Desktop:

- repo: `E:\codex\hope-desktop-shell`
- branch: `codex/desktop-shell`
- anchor: `66d687d` (`desktop-shell: add writer qwen input readiness boundary`)
- state: accepted and waiting

Controlled intake:

- repo: `E:\codex\hope-intake-app`
- branch: `codex/controlled-intake-snapshot-bootstrap-app`
- anchor: `d74bd11` (`qwen-storyboard: tighten contract boundary`)
- state: accepted and waiting

## Readiness Inputs Now Available

The following required planning inputs are now Git-visible:

- RC publication smoke confirmation:
  `docs/rc-publication-confirmation-2026-04-22.md`
- KB golden-sample staging acceptance:
  `docs/kb-golden-sample-intake-review-2026-04-22.md`
- V3 golden-sample overlay acceptance:
  `docs/v3-golden-sample-overlay-review-2026-04-22.md`
- KB normalized staging artifact:
  `E:\codex\hope-kb\docs\normalized-staging\golden-sample-library-642bd7876e0d4649814f35fb133b67f3-2026-04-22.normalized.json`
- V3 proposal branch source materials:
  `origin/codex/v3-field-overlay-proposal @ 7a08cd6`

## Gate Status

`V0_2_SCOPE_GATE_READY_FOR_CONTROL_DECISION`

The project is ready for a control decision about opening v0.2, but the gate is
not opened by this memo.

Current RC line remains protected:

- v0.1 17-sheet workbook remains frozen
- current exporter behavior remains frozen
- current IPC remains frozen
- current Rust DTOs and validators remain frozen
- live Qwen and Seedance remain closed
- `hope-kb` remains separate from `hope`

## Proposed v0.2 Work Order

If the main control thread opens v0.2 implementation, use this order.

### 1. KB Schema Package

Thread:

- `Hope-KB-GoldenSampleLibraryV0.2【SchemaGate执行中】`

Owner:

- existing KB thread `019da89f-49a2-7e11-bd2f-c0138165fdd9`

Goal:

- promote the staging-only golden sample artifact into an explicit v0.2 KB
  schema package, without touching `hope` product code

Expected outputs:

- `seed/v0.2/golden_sample_library.json`
- `seed/v0.2/golden_sample_field_coverage_rules.json`
- `seed/v0.2/golden_sample_failure_mapping.json`
- `seed/v0.2/golden_sample_repair_mapping.json`
- source register / provenance entries
- validation and snapshot-readiness notes for v0.2

Hard boundary:

- do not modify `hope`
- do not merge into v0.1 seed
- do not alter current v0.1 snapshot contract

### 2. V3/V4 Contract Freeze Candidate

Thread:

- `Hope-V3字段线程-GoldenSampleContract【v0.2 Freeze候选】`

Owner:

- existing V3 field thread `019db40b-4299-75c3-8ea7-d6b1ff3a8175`

Goal:

- turn the accepted proposal fields into a v0.2 contract-freeze candidate after
  the KB v0.2 package shape is known

Expected outputs:

- final field list for `golden_sample_library`
- relation to V3 core fields and V4 overlay profiles
- explicit future `reference_control_core` coverage gap
- implementation prerequisites for Hope validator / export / desktop

Hard boundary:

- docs-only unless main control explicitly opens implementation

### 3. Hope Validator / Domain Planning

Thread:

- `Hope主线-GoldenSampleValidatorPlanning【v0.2待开门】`

Owner:

- Hope main integrator after the v0.2 scope gate opens

Goal:

- plan how Hope will consume `golden_sample_library` as validator evidence,
  repair input, and Qwen few-shot retrieval metadata

Expected outputs:

- DTO / domain change list
- validator gate list
- repair mapping use list
- fixture / benchmark impact list

Hard boundary:

- no code until the scope gate explicitly says implementation is open

### 4. Controlled Intake / Qwen Boundary

Thread:

- `Hope接入线程-GoldenSampleRetrievalBoundary【Qwen边界规划】`

Owner:

- existing intake thread `019daa9c-1955-7852-92ea-e9a2139acaef`

Goal:

- define how future Qwen content generation may retrieve positive few-shot
  samples while excluding negative examples from prompt context

Hard boundary:

- no live Qwen connection
- no prompt generation implementation until later gate

### 5. Desktop Readonly Provenance

Thread:

- `Hope桌面端-GoldenSampleProvenance【Readonly规划】`

Owner:

- existing desktop thread `019daa78-87fb-7380-a8be-c19a7bf168b7`

Goal:

- plan read-only provenance display for future generated output and validation
  findings

Hard boundary:

- no new desktop UI package until main control opens that gate

## Next Dispatch If The Gate Opens

The first implementation-facing dispatch should go to the existing KB thread,
not to Hope main:

```text
线程名：Hope-KB-GoldenSampleLibraryV0.2【SchemaGate执行中】
线程ID：019da89f-49a2-7e11-bd2f-c0138165fdd9
仓库 / 分支：E:\codex\hope-kb / codex/contracts-freeze
当前锚点：21d5641 Normalize golden sample intake staging

本轮任务：
在 hope-kb 内建立 v0.2 golden_sample_library schema package，把已 normalized 的
40 条黄金样本从 staging-only 规划为 v0.2 seed/schema 资产。保留全部 17 个源字段、
provenance、few-shot eligibility、validator evidence、negative-sample signal、
repair mapping planning 和 V3 core coverage。

禁止范围：
- 不修改 E:\codex\hope
- 不合流到 hope
- 不改 v0.1 seed / manifest / import_map / snapshot contract
- 不改 desktop / intake
- 不接 Qwen / Seedance

完成后回报：
- final branch / commit
- changed files
- v0.2 schema files
- record counts
- validation result
- snapshot/readiness result
- 哪些字段仍需 Hope validator/export/desktop 后续 gate
```

## Current Decision

Until the user or main control explicitly says to open the v0.2 scope gate, all
threads stay in the following states:

- `Hope主线-RCPublicationConfirmation【RC_READY守护中】`
- `Hope-KB-黄金样本库Staging【知识库革命·已推送】`
- `Hope-V3字段线程-GoldenSampleOverlay【v0.2 Proposal已接收待命】`
- `Hope桌面端-WriterReadiness【InputBoundary待命】`
- `Hope接入线程-Qwen分镜生成Contract【Contract边界待命】`
