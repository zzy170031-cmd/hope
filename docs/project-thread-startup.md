# Hope Project Thread Startup

Use this prompt when opening the dedicated Hope implementation thread:

```text
本线程专门用于 Hope 项目实施。启用 five-agent-governance 真实五代理模式，并按 Hope 最终封版方案执行。当前阶段为 Day 1–3 契约冻结。请以主线程作为唯一集成者，按 8 Track / 4 Lane 滚动并行规则推进，并以 E:\\codex\\hope 作为主仓库、E:\\codex\\hope-kb 作为知识库仓库。
```

## Codex 使用核心规则

所有跨线程沟通、总控指令、线程回报、归档包、接力包和验收清单，
默认遵循 [`docs/codex-usage-core-rules.md`](codex-usage-core-rules.md)。

## Thread intent

- one project thread per major milestone
- one integration thread that never becomes a feature branch dumping ground
- one benchmark owner in the main thread

## Suggested next thread sequence

1. contract-freeze thread
2. wave-1 implementation thread
3. wave-2 implementation thread
4. integration / E2E thread
5. release candidate thread
